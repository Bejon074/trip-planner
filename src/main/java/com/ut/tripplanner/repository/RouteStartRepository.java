package com.ut.tripplanner.repository;

import com.ut.tripplanner.domain.RouteStart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RouteStartRepository extends JpaRepository<RouteStart, Long> {
}

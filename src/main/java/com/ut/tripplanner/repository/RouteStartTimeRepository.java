package com.ut.tripplanner.repository;

import com.ut.tripplanner.domain.RouteStartTime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RouteStartTimeRepository extends JpaRepository<RouteStartTime, Long> {
}
